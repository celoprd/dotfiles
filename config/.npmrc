# ===== NPM CONFIGURATION =====

# Set npm registry to GitHub Packages for @skelloapp scope
@skelloapp:registry=https://npm.pkg.github.com

# Default registry for other packages
registry=https://registry.npmjs.org/

# GitHub Packages authentication (set your token in .env.zsh and it will be available)
# The token will be read from GITHUB_TOKEN environment variable
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Save prefix for global installations
prefix=${HOME}/.npm-global

# Make npm installs faster
# Disable package-lock.json if you prefer (not recommended for production)
# package-lock=false

# Set default init values
init-author-name=Celo
init-license=MIT
init-version=1.0.0

# Logging
loglevel=warn

# Always use exact versions when saving dependencies
save-exact=true

# Disable update notifier (optional)
# update-notifier=false

# Set default node version if needed (for projects without .nvmrc)
# engine-strict=true

# ===== PERFORMANCE SETTINGS =====

# Use legacy peer deps if you encounter dependency conflicts
# legacy-peer-deps=true

# Reduce network requests
# prefer-offline=true
# prefer-online=false
